# 2026-07-26 아키텍처 변경 사항

## 1. 변경된 핵심 구조

기존 구조:

```text
User → UserVault → 개별 투자 Agent
```

변경 구조:

```text
외부 Signal Provider들
        │
        │ x402 유료 API
        ▼
AgoraAgent
        │ 신호 검증·조합·위험 평가
        ▼
UserVault
        │
        ▼
허용된 DEX / Pool
```

- 각 사용자 Vault에는 개별 외부 Agent를 연결하지 않는다.
- 모든 Vault의 거래 실행 권한은 Agora가 운영하는 `AgoraAgent`에만 부여한다.
- 외부 Agent는 Vault 실행자가 아니라 투자 신호를 제공하는 `Signal Provider`다.
- AgoraAgent는 x402를 통해 Signal Provider의 신호를 구매한다.
- Signal Provider는 사용자 Vault, Coin, 거래 실행 권한에 직접 접근할 수 없다.

## 2. AgoraAgent의 역할

AgoraAgent는 여러 Signal Provider의 응답을 받아 다음 작업을 수행한다.

- 신호의 생성 시각과 만료 여부 확인
- 여러 Provider의 신호 교차 검증
- 과거 성과와 실시간 성과 추적
- 비정상 수익률, 조작 신호, 반복 실패 탐지
- 신뢰도가 낮아진 Provider의 신호 제외 및 x402 구매 중단
- 사용자 Vault의 위험 한도 안에서 최종 거래 수량 계산
- Vault의 허용된 함수를 사용해 거래 요청 또는 DEX 거래 실행

Signal Provider가 악의적인 신호를 보내더라도 직접 Vault 자산을 이동할 수는 없다.

## 3. 온체인 권한 모델

`UserVault`의 권한은 다음처럼 구분한다.

| 주체 | 허용 권한 |
| --- | --- |
| Vault owner | 입금, 출금, 한도 변경, AgoraAgent 중지·교체 |
| AgoraAgent | 제한된 BUY/SELL 요청 또는 실행 |
| Signal Provider | 온체인 Vault 권한 없음 |
| 그 외 주소 | Vault 거래 및 설정 변경 권한 없음 |

AgoraAgent가 탈취되더라도 owner 전용 출금 함수를 호출할 수 없으므로 공격자 지갑으로 Vault 자산을 직접 출금할 수 없다.

다만 실제 DEX 거래가 추가되면 악성 거래로 자산 가치를 훼손할 가능성은 남는다. 따라서 실제 실행 함수는 다음 불변식을 온체인에서 강제해야 한다.

- 거래 결과의 수령자는 항상 해당 Vault
- Agent가 임의의 수령자 주소를 지정할 수 없음
- 입력·출력 Coin 타입 고정
- 허용된 DEX와 Pool만 사용
- 최소 수령량(`min_amount_out`) 강제
- 거래 만료 시간(`deadline`) 강제
- 1회 및 epoch 누적 거래 한도 강제
- 거래 성공 시에만 Vault 잔액과 누적 사용량 갱신

## 4. AgoraAgent 실행 상태

위험 감지 후 무조건 전체 기능을 끄는 대신 다음 세 상태를 사용한다.

```text
ACTIVE
├─ BUY 가능
└─ SELL 가능

REDUCE_ONLY
├─ BUY 불가능
└─ SELL 가능

PAUSED
├─ BUY 불가능
└─ SELL 불가능
```

- 정상 상태는 `ACTIVE`다.
- Signal Provider의 이상이나 시장 위험이 감지되면 `REDUCE_ONLY`로 전환해 신규 투자를 막고 포지션 축소만 허용할 수 있다.
- 심각한 장애나 운영 키 사고가 발생하면 `PAUSED`로 전환한다.
- 상태 변경 권한은 1차적으로 Vault owner에게 둔다.

## 5. Agent 타입과 Dynamic Bag 결정

외부 Agent를 온체인 실행 객체로 Vault에 저장하지 않으므로 다음 문제는 제거된다.

- `Agent<T>` 타입별 Vault 연결
- Agent 종류별 제네릭 처리
- 여러 Agent 객체를 담기 위한 Dynamic Field 또는 Bag

단, 이것은 Agent 저장 문제에 대한 결론이다. Vault가 여러 종류의 투자 자산을 동시에 보유하려면 자산 저장을 위한 Dynamic Field 또는 Bag 문제는 별도로 남는다.

1차 MVP에서는 다음 정책을 사용한다. 단, 거래쌍은 사용자가 선택하지 않고 Agora 배포 설정이 결정한다.

```text
Vault 하나 = Agora가 지정한 FiatT/CryptoT 거래쌍 하나

예:
UserVault<USDC, SUI>
UserVault<USDC, DEEP>
```

Vault Transaction 빌더의 공개 입력에서는 `coinType`과 `cryptoCoinType`을 제거한다. 빌더는 다음 배포 환경 설정만 사용한다.

```text
NEXT_PUBLIC_AGORA_FIAT_COIN_TYPE
NEXT_PUBLIC_AGORA_CRYPTO_COIN_TYPE
NEXT_PUBLIC_AGORA_AGENT_OPERATOR
```

사용자는 USDC 예치 금액만 정하며 Fiat 타입, Crypto 타입 또는 AgoraAgent 주소를 전달하지 않는다. 따라서 1차 구현에서는 Dynamic Bag을 사용하지 않는다. AgoraAgent가 한 Vault에서 여러 Crypto를 선택해야 하는 시점에는 다중 자산 저장 구조를 별도로 설계한다.

## 6. 1차 컨트랙트 변경 범위

- `agent_operator`의 의미를 AgoraAgent 운영 주소로 고정
- Vault 내부 필드와 이벤트에서 `agora_agent_operator` 명칭 사용
- 외부 Signal Provider는 Vault 권한 검사 대상에서 제외
- `ACTIVE`, `REDUCE_ONLY`, `PAUSED` 상태 추가
- `REDUCE_ONLY`에서는 BUY 차단, SELL 허용
- 기존 프론트엔드 호환을 위해 `revoke_agent`, `replace_agent`, `reactivate_agent` 공개 함수 이름은 당분간 유지
- 현재 `request_buy/request_sell`은 실제 자산을 이동하지 않는 요청 단계로 유지
- `sources/AgoraAgent/AgoraInvest.move`를 별도 생성
- AgoraInvest에서 `signal_digest`, `risk_score_bps`를 검사·기록한 뒤 Vault BUY/SELL 검사를 호출
- `AgoraBuyDecisionRecorded`, `AgoraSellDecisionRecorded` 이벤트 추가

## 7. 다음 구현 단계

1. AgoraInvest를 공식 유일 BUY/SELL 실행 경로로 제한
2. Signal Provider ID와 전략 버전을 signal digest 실행 기록에 연결
3. Mock DEX 기반 `execute_buy/execute_sell` 원자적 정산 구현
4. `min_amount_out`, `deadline`, DEX/Pool allowlist 추가
5. 실제 거래 성공 시에만 epoch 누적량이 증가하도록 변경
6. 운영 키 KMS 관리, dry-run, 비정상 거래 자동 중지 적용

## 8. Follow와 사용자 Agent 선택 제거

사용자는 더 이상 Marketplace에서 투자 Agent를 고르거나 follow하지 않는다.

```text
기존:
회원가입 → Agent 검색 → Agent 선택 → Crypto 선택 → Follow fee 결제 → Vault 연결

변경:
회원가입 → 지갑 연결 → 개인 Vault 생성·USDC 예치 → Agora 자동 운용
```

- `FollowRecord`, `follow_agent`, `follow_fee_mist`를 제거한다.
- 사용자는 Signal Provider를 선택하지 않는다.
- 사용자는 Fiat 또는 투자 대상 Crypto 타입을 직접 선택하지 않는다.
- 1차 MVP의 CryptoT는 Agora 배포 설정이 지정한다.
- AgoraAgent가 내부 정책과 위험 평가를 통해 Signal Provider와 투자 대상을 결정한다.
- 외부 Signal Provider 등록 정보에는 follow fee가 없고 x402 결제 수령 주소만 존재한다.
- x402 신호 구매 비용과 결제 트랜잭션 가스는 사용자가 아니라 AgoraAgent가 부담한다.

## 9. 사용자 가스비 정책

지갑 연결과 일반 회원가입은 온체인 트랜잭션이 아니므로 가스비가 발생하지 않는다.

사용자가 직접 가스를 부담하는 기본 작업:

- 최초 UserVault 생성 및 AgoraAgent 연결
- Vault에 USDC 예치
- Vault 자산을 사용자 Wallet으로 출금

AgoraAgent 또는 플랫폼이 가스를 부담하는 작업:

- Signal Provider에 대한 x402 신호 구매 결제
- 자동 BUY/SELL 요청과 향후 DEX 실행
- 신호 검증 결과와 거래 실행 기록

정확한 표현은 “자동 투자에 가스가 발생하지 않는다”가 아니라 “자동 투자 트랜잭션의 가스를 AgoraAgent가 부담하므로 사용자 지갑에서는 차감되지 않는다”이다.

Owner가 직접 `PAUSED`, `REDUCE_ONLY`, 한도 변경 등을 온체인에서 실행하면 해당 트랜잭션에는 가스가 필요하다. 이 작업도 사용자 무가스로 제공하려면 플랫폼의 sponsored transaction 정책을 추가해야 한다.
